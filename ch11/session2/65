		
		2xy/(x² + y²),		(x, y) != (0, 0)
f(x, y) =
		0,			(x, y) == (0, 0)


(a)把m = tan θ 代入公式
	f(x, y) | y = mx  = 2m/(1 + m²) = 2tan θ / (1 + tan²θ) = 2sinθ cosθ = sin(2θ)

(b)于是当沿直线y = mx趋近于(0, 0)时, f(x, y)的值在(-1, 1)之间变化, 故没有极限
