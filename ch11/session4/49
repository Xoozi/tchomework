微分积分

在适当条件下, 若F(x) = ∫(a, b) g(t, x) dt, 则F'(x) = ∫(a, b) gx(t, x) dt

	利用这个事实和链式法则求:

	F(x) = ∫(a, f(x)) g(t, x) dt

	的导数, 为此令:

	G(u, x) = ∫(a, u) g(t, x) dt

	其中u = f(x)


49. F(x) = ∫(0, x²) sqrt(t**4 + x**3) dt

	
	u = x²
	g(t, x) = sqrt(t**4 + x**3)
	∂g/∂x = 3x/sqrt(t**4 + x**3)

	F'(x) = ∫(0, x²) 3x/sqrt(t**4 + x**3) dt

	xoozi这个有点玄幻, 现在生病, 后面再看
