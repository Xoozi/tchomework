求下列每个区域绕y轴旋转所产生的立体体积

39 区域由顶点(1, 0), (2, 1), (1, 1)的三角形围成
	
	R(y) 	= y + 1
	r(y) 	= 1

	A(y)	= pi*(R(y)**2 - r(y)**2) = pi*(y**2 + 2*y) = 

	V	= ∫(0, 1) A(y) dy = pi*∫(0, 1) y**2 + 2*y dy
		F(y) = y**3/3 + y**2
		= pi*(F(1) - F(0))
		= pi*(1/3 + 3/3) = 4*pi/3


41 指定区域在第一像限, 其上方是抛物线 y = x**2, 下方是x轴且右边是直线x = 2
	
	R(y) 	= 2
	r(y)	= sqrt(y)
	A(y)	= pi*(R(y)**2 - r(y)**2) = pi*(4 - y)

	V	= ∫(0, 4) A(y) dy = pi*∫(0, 4) 4 - y dy
		F(y) = 4*y - y**2/2
		= pi*(F(4) - F(0))
		= pi*(16 - 8) = 8*pi
