螺旋线的最大曲率

在之前我们得到r(t) = (a cos t)i + (a sin t)j + btk (a, b >= 0)的曲率是:

κ = a/(a² + b²)

对于给定的b, 最大的κ值是多少?

将κ写成a的函数

κ(a) = a/(a² + b²)

κ'(a) = ((a² + b²) - a(2a))/(a² + b²)² = (b² - a²)/(a² + b²)²

解κ'(a) = 0
    得a = b 或a = -b

    κ(b) = b/(2b²) = 1/2b
    κ(-b) = -b/(2b²) = -1/2b

