切片法求体积

立体夹在x = -1和x = 1垂直于x轴的两平面之间, 垂直于x轴的横截面是

(a)圆, 其直径从曲线y = -1/sqrt(1+x**2) 伸展到y = 1/sqrt(1 + x**2)

	D(x) = 2/sqrt(1 + x**2)
	R(x) = 1/sqrt(1 + x**2)
	A(x) = pi*R**2 = pi/(1 + x**2)

	V = ∫(-1, 1) pi/(1 + x**2) dx
	  = pi * arctan(x)|(-1, 1)
	  = pi*(pi/4 - -pi/4)
	  = pi*pi/2

(b)竖直正方形, 其底边从曲线y= -/sqrt(1+x**2) 伸展到y = 1/sqrt(1+x**2)
	L(x) = 2/sqrt(1 + x**2)
	A(x) = 4/(1+x**2)

	V = ∫(-1, 1) 4/(1+x**2) dx
	  = 4 * arctan(x)|(-1, 1)
	  = 4 * (pi/4 - -pi/4)
	  = 2pi
