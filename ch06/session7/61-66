当计算器上没有反双曲函数键时, 仍可以通过像下面那样把它们表成对数来求反双曲函数的值

asinh(x) = ln(x + sqrt(x² + 1))
acosh(x) = ln(x + sqrt(x² - 1)), x >= 1
atanh(x) = 1/2 * ln((1 + x)/(1 - x)), |x| < 1
acoth(x) = 1/2 * ln((1 + x)/(1 - x)), |x| > 1

