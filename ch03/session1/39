设f(x) 		= (x - 2)**2/3

	f'(x) 	= 2/3*(x - 2)**-1/3
		= 2/(3*(x - 2)**1/3)

(a)f'(2)存在吗
	f'(2)是不存在的, 因为它会导致除零	

(b)证明f仅有的极值在x=2处取到
	由于f'(x)是不可能得到0的, 故临界点只能是使f'(x)无定义的x=2处
	而f(x)在x=2处有定义, 这样就可以得出, f仅有的极值在x=2处取得

(c)(b)中的结果和极值定理矛盾吗?
	不矛盾, 也许应该说,完全符合极值定理

(d)用a代替2, 对f(x) = (x - a)2/3, 重做(a)和(b)
	f'(a)不存在, 因为它会导致除零
	由于f'(x)是不可能得到0的, 故临界点只能是使f'(x)无定义的x=a处
	而f(x)在x=a处有定义, 这样就可以得出, f仅有的极值在x=a处取得

