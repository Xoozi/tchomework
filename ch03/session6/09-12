作为近似的线性化
选择中心不在x = a的线性化, 但在x = a 附近容易计算函数及导数值

9 f(x) = 2*x**2 + 4*x - 3,  a = -0.9

    L(x) = f(a) + f'(a) * (x - a),      选择-1
         = 2 - 4 - 3 + (-4 + 4)*(x + 1)
         = -5 

11 f(x) = x / (x + 1),  a = 1.3
    L(x) = f(a) + f'(a) * (x - a)
         = a / (a + 1) + ((a+1)- a) / (a+1)**2 * (x - a)
         = a/(a + 1) + 1 / (a+1)**2 * (x - a)
         选择 a = 1
         = 1/2 + 1/4 * (x - 1)
