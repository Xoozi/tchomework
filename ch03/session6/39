铸造硬币
制造商和联邦政府签订铸造硬币的合同. 如果铸造的硬币重量在理想重量的1/1000范围内是容许的
试问此硬币的半径容许偏差为多少? 假设硬币的厚度不变

    硬币的重量和硬币的体积成正比, 所以此问题转化为与理想体积偏差1/1000内时
    硬币半径的容差
    V(r) = h*pi*r**2
    dV = V'(r)*dr = 2*h*pi*r*dr
    dV/V = (2*h*pi*r*dr)/(h*pi*r**2) = 2*dr/r <= 1/1000
    =>dr/r <= 1/2000
