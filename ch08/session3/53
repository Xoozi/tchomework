重新编号

例5中的级数可以写成∑(n=1, ∞) 1/((n + 1)(n + 2)) 和∑(n=-1, ∞) 1/((n + 3)(n + 4))

以下列n作为开始编号, 重新写级数

(a) n = -2
	
	∑(n=-2, ∞) 1/((n + 4)(n + 5))

(b) n = 0
	
	∑(n=0, ∞) 1/(n(n + 1))

	xoozi居然吧这个写错了, 应该是∑(n=0, ∞) 1/((n + 2)(n + 3))

(c) n = 5

	∑(n=5, ∞) 1/((n - 3)(n - 2))
