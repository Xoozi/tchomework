写出级数的头几项以求得a和r, 并求级数的和, 然后通过不等式|r| < 1 求使不等式成立从而使级数收敛的x值

33 ∑(n=0, ∞) (-1)**n x**n
	= ∑(n=1, ∞) (-1)**(n-1) x**(n-1)
	= ∑(n=1, ∞) (-x)**(n-1)

	: 1, -x, x², -x³, ..., (-x)**(n-1)

	a = 1, r = -x

	|r| < 1
	=> |-x| < 1
	=> -1 < -x < 1
	=> -1 < x < 1

35 ∑(n=0, ∞) 3((x - 1)/2)**n

	= ∑(n=1, ∞) 3((x - 1)/2)**(n-1)

	a = 3, r = (x - 1)/2

	:3, 3r, 3r², 3r³, ..., 3r**(n-1)

	|r| < 1
	=> -1 < (x - 1)/2 < 1
	=> -2 < x - 1 < 2
	=> -1 < x < 3
