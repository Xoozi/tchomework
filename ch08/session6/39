逐项求导

对x的哪些值, 级数:

	1 - 1/2(x - 3) + 1/4(x - 3)² + ... + (-1/2)**n (x - 3)**n + ...

	= ∑(n=0, ∞) (-(x - 3)/2)**n

	比值法: | Un+1 / Un | = | - (x - 3)/2 | < 1
	=> |(3 - x)/2| < 1
	=> -2 < 3 - x < 2
	=> -5 < -x < -1
	=> 1 < x < 5

	对x = 1 和x = 5, 级数都发散

	故, 收敛区间是 (1, 5), 和为 1/(1 + (x - 3)/2) = 2 / (x - 1)

	如果逐项求导, 会得到∑(n=1, ∞) n(-(x - 3)/2)**(n - 1)

	收敛区间和以前一样, 和为(2 / (x - 1))' = -2/(x - 1)²
